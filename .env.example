# Copy to .env
# Token from Cloudflare Zero Trust -> Networks -> Tunnels -> your tunnel -> Docker snippet
CLOUDFLARE_TUNNEL_TOKEN=
# Optional: server-side Anthropic key so AI plant lookup works for every user (no key in browsers)
ANTHROPIC_API_KEY=
# Set to false to stop new account sign-ups once your household is registered
REGISTRATION_OPEN=true
